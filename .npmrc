# The runtime image is node:14-bullseye, whose npm 6 only understands
# lockfileVersion 1. A v2/v3 shrinkwrap written by a modern npm has no
# "dependencies" key, and `npm ci` dies with
# "Cannot read property '<pkg>' of undefined".
lockfile-version=1
